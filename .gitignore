# Rust build artifacts
/target

# Cargo lock file (optional — many apps commit it, but if you don't want it)
/Cargo.lock

# Backup files from some editors
**/*.rs.bk

# Your personal config (don't commit personal settings)
/config.toml

# macOS junk
.DS_Store

# IDE/project files (if you use any)
/.idea
/.vscode
